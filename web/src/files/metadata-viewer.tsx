// import {metadataViewRegistry} from "../../contents/types/type-registries.ts"
// import useContent from "../../contents/use-content.ts"

export default function MetadataViewer() {
	// let [file, changeFile] = useFile(fileId)
	// if (!file) {
	return <div class="metadata metadata--loading metadata--loading-file" />
	// }
	// 	let [content, changeContent] = useContent(file.content)
	// 	if (!content) {
	// 		return <div class="metadata metadata--loading metadata--loading-content" />
	// 	}

	// 	let [MetadataView] = metadataViewRegistry.get(content.contentType)

	// 	if (!MetadataView) {
	// 		return null
	// 	}

	// 	return (
	// 		<MetadataView
	// 			content={content}
	// 			file={file}
	// 			changeContent={changeContent}
	// 			changeFile={changeFile}
	// 		/>
	// 	)
	// }
}
