import {useRoute} from "wouter-preact"
import {CardLinkProps, CardLink} from "./card.tsx"

export default function ({href, icon, title}: CardLinkProps) {
	// useRoute()
	return <CardLink {...props} />
}
